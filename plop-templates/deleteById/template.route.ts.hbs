
    /*
     * @swagger
     * /api/{{ kebabCase resource }}s/{id}:
     *   delete:
     *     tags: ["{{ pascalCase resource }}s"]
     *     summary: Delete one {{ pascalCase resource }} by id
     *     security:
     *      - bearerAuth: []
     *     produces:
     *      - application/json
     *     parameters:
     *      - in: path
     *        name: id
     *        type: number
     *        required: true
     *     responses:
     *       204:
     *         description: Success
     *       401:
     *         description: Unauthorized
     *       403:
     *         description: Forbidden
     *       500:
     *         description: Error during query execution
     */
    this.api.delete(
      "/:id",
      this.{{ camelCase resource }}sController.delete{{ pascalCase resource }}ById.bind(this.{{ camelCase resource }}sController)
    );