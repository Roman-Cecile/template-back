
    /*
     * @swagger
     * /api/{{ kebabCase resource }}s/{id}:
     *   get:
     *     tags: ["{{ pascalCase resource }}s"]
     *     summary: Get one {{ pascalCase resource }}
     *     security:
     *      - bearerAuth: []
     *     produces:
     *      - application/json
     *     parameters:
     *      - in: path
     *        name: id
     *        type: number
     *        required: true
     *     responses:
     *       200:
     *         description: Success
     *       401:
     *         description: Unauthorized
     *       404:
     *         description:  {{ pascalCase resource }} not found
     *       500:
     *         description: Error during query execution
     */
    this.api.get(
      "/:id",
      this.{{ camelCase resource }}sController.get{{ pascalCase resource }}.bind(
        this.{{ camelCase resource }}sController
      )
    );